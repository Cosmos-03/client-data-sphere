spring.application.name=client-data-sphere-backend

# Server configuration
server.port=8080

# AWS S3 configuration (use environment variables or a secure secrets manager in production)
aws.access.key=${AWS_ACCESS_KEY:your_access_key}
aws.secret.key=${AWS_SECRET_KEY:your_secret_key}
aws.region=us-east-1

# Logging
logging.level.org.springframework=INFO
